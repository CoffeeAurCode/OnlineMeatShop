/**
 * Placeholder. The storefront is increment 7b.
 *
 * This page exists so increment 0 has something to deploy and so the
 * staging pipeline can be proven end to end before any real code depends
 * on it.
 */
export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '3rem', lineHeight: 1.6 }}>
      <h1>Online Meat Shop</h1>
      <p>Foundation deployed. Storefront lands in increment 7b.</p>
    </main>
  );
}
